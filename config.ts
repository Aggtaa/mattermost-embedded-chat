import { ChatServerOptions } from "./src/chatServer";

const options = {
    port: 8087,
    botToken: 'create a permanent bot token',
    teamName: 'team name',
    staticChannelName: 'channel name'
    
} as ChatServerOptions;

export default options;